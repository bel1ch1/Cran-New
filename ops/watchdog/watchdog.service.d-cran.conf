[Unit]
After=docker.service network-online.target
Wants=docker.service network-online.target

[Service]
EnvironmentFile=-/etc/cran/watchdog.env
# Give Docker a head start before watchdog daemon begins test-binary checks.
ExecStartPre=/bin/sleep 30
